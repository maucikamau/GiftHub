Tablica 1. Odabrana ispitivanja:

| Red. br. | Razina ispitivanja | Funkcionalnost | Tip slučaja | 
| :--- | :--- | :--- | :--- | 
| 1. | Komponenta | [Stvaranje kampanje s neispravnom lokacijom](#test1)  | Rubni uvjet |  
| 2. | Komponenta | [Slanje nevažeće poruke u Chat-u](#test2) | Nepostojeća funkcionalnost |  
| 3. | Komponenta | [Računanje recenzije korisnika](#test3) | Redovni slučaj | 
| 4. | Komponenta | [Ponovno slanje zahtjeva](#test4) | Izazivanje pogreške | 
| 5. | Komponenta | [Neovlašteno stvaranje kampanje](#test5) | Zabranjena funkcionalnost | 
| 6. | Komponenta | [Skrivanje oglasa](#test6) | Redovni slučaj | 
| 7. | Sustav | [Lokalna registracija i uređivanje profila](#test7) | Redovni slučaj |  
| 8. | Sustav | [Prijava i odjava s OAuth 2.0](#test8) | Redovni slučaj |
| 9. | Sustav | [Objava, uređivanje i brisanje oglasa](#test9) | Redovni slučaj |  
| 10. | Sustav | [Objava, uređivanje i brisanje kampanji](#test10) | Redovni slučaj | 
| 11. | Sustav | [Slanje i primanje poruka i zahtjeva putem chata](#test11) | Redovni slučaj | 
| 12. | Sustav | [Administratorsko sučelje - uklanjanje sadržaja](#test12) | Redovni slučaj | 
| 13. | Sustav | [Upis igračaka istog imena u kampanju](#test13) | Nepostojeća funkcionalnost |
| 14. | Sustav | [Vršenje plaćanja dostave i ostavljanje potvrde i recenzije](#test14) | Redovni slučaj |  
| 15. | Sustav | [Polovično izvršeno plaćanje](#test15) | Rubni slučaj | 

&nbsp;

# Ispitivanje komponenti
> Provedena ispitivanja su napravljena uz pomoć alata _pytest_

## <a id="test1"> TEST 1: Stvaranje kampanje s neispravnom lokacijom </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| objekt Kampanja | 1. Kreirati objekt Kampanja s neispravnom lokacijom (npr. _Integer_ vrijednost 9999). <br> 2. Poslati POST zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom. | status.HTTP_400_BAD_REQUEST | PASS |

<details><summary> kod </summary>

```python

def test_create_campaign_invalid_location(self):
     #Test with non-existent location ID
     self.client.force_authenticate(user=self.recipient_association)
     data = {
        'title': 'Fail', 'description': '...',
        'location': 9999,
        'wish_list': []
        }

     response = self.client.post(self.create_url, data, format='json')
     self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)
```

</details>

## <a id="test2"> TEST 2: Slanje nevažeće poruke (Chat) </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| korisnik Darovatelj, <br> neispravni ID poruke | 1. Autentificirati Darovatelja. <br> 2. Stvoriti nevažeći ID poruke i pokušati poslati ju. <br> 3. Poslati POST zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom. | status.HTTP_404_NOT_FOUND | PASS |

<details><summary> kod </summary>

```python

@patch('backend.chat.views.StreamChat')
def test_respond_delivery_request_invalid_msg_id(self, MockStreamChat):
    self.client.force_authenticate(user=self.donor)

    invalid_msg_id = "nonexistent-msg-id"
    respond_url = reverse('chat:respondRequest', kwargs={'msg_id': invalid_msg_id})
    data = {'check': True}

    response = self.client.post(respond_url, data)

    self.assertEqual(response.status_code, status.HTTP_404_NOT_FOUND)
```
</details>

## <a id="test3"> TEST 3: Recenzija korisnika </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| korisnik Primatelj, <br> oglasi, <br> recenzije | 0. Kreirati klasu sa darovateljem, primateljem, oglasom i sl. <br> 1. Kreirati barem 2 oglasa za istog darovatelja i toliko recenzija. <br> Izračunati prosjek ocjena. <br> 2. Autentificirati Primatelja. <br> 3. Poslati GET zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom. | status.HTTP_200_OK, <br> vrijednost izračunatog prosjeka | PASS |

<details><summary> kod </summary>

```python

def test_get_average_rating(self):
            
      # Create two reviews for the donor
      listingOver2 = Listing.objects.create(title='Drugi', owner=self.donor, is_active=False)

      Review.objects.create(donor=self.donor, reviewer=self.recipient, for_listing=self.listingOver, rating=5)
      Review.objects.create(donor=self.donor, reviewer=self.recipient, for_listing=listingOver2, rating=3)

      self.client.force_authenticate(user=self.recipient)
      response = self.client.get(self.avg_url)

      self.assertEqual(response.status_code, status.HTTP_200_OK)
      self.assertEqual(response.data['average'], 4.0)
```

</details>

## <a id="test4"> TEST 4: Ponovno slanje zahtjeva </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| korisnik Primatelj, <br> objava, <br> objekt Zahtjev | 0. Kreirati klasu s korisnicima, objavom i ostalo. <br> 1. Autentificirati Primatelja. <br> 2. Uspostaviti ChatChannel za Primatelja i oglas. <br> 3. Kreirati novi zahtjev (ChatChannel) za isti oglas. <br> 4. Potvrditi donaciju i spremiti. <br> 5. Poslati POST zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom.| status.HTTP_400_BAD_REQUEST | PASS |

<details><summary> kod </summary>

```python

   def test_create_delivery_request_already_sent(self):

        self.client.force_authenticate(user=self.recipient)
        channel_id = f"{self.listing1.id}-{self.recipient.chat_uid}"

        # Prvo kreiramo zahtjev za dostavu
        self.chat = ChatChannel.objects.create(
            stream_channel_id=channel_id,
            listing=self.listing1,
            donor=self.donor,
            recipient=self.recipient,
            delivery_check=True
        )

        self.listing1.confirmed_donation_conversation = self.chat
        self.listing1.save()


        data = {'delivery_type': 'shipping'}
        response = self.client.post(self.delivery_req_url, data)

        self.assertEqual(response.status_code, status.HTTP_400_BAD_REQUEST)

```
</details>

## <a id="test5"> TEST 5: Neovlašteno stvaranje kampanje </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| objekt Kampanja, <br> korisnik Primatelj | 1. Autentificirati primatelja. <br> 2. Kreirati objekt Kampanja. <br> 3. Poslati POST zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom. | status.HTTP_403_FORBIDDEN | PASS |

<details><summary> kod </summary>

```python

def test_create_campaign_unauthorized_user(self):
        
        self.client.force_authenticate(user=self.recipient)

        data = {
            'title': 'Summer clothes',
            'description': 'This should fail',
            'location': self.location.id,
            'wish_list': [{"name": "Trunks", "count": 5, "donated": 0}],
            'end_date': '2026-12-31T23:59:59Z'
        }

        response = self.client.post(self.create_url, data, format='json')
        self.assertEqual(response.status_code, status.HTTP_403_FORBIDDEN)
```

</details>

## <a id="test6"> TEST 6: Skrivanje oglasa </a>
| Ulazi | Koraci | Očekivani izlaz | Dobiveni izlaz |
| :--- | :--- | :--- | :--- |
| korisnik Primatelj, <br> oglas | 1. Potvrditi donaciju za oglas i spremiti. <br> 2. Autentificirati Primatelja. <br> 3. Poslati GET zahtjev na server, pohraniti odgovor u _response_ i usporediti ga s očekivanim izlazom.| status.HTTP_200_OK, <br> _count = 0_ | PASS |

<details><summary> kod </summary>

```python

def test_listing_without_confirmed_conversation_not_shown(self):
        # Test that listings with confirmed conversations are not shown.
        # Remove the active confirmed donation conversation
        self.listing1.confirmed_donation_conversation = None
        self.listing1.save()

        self.client.force_authenticate(user=self.recipient)
        response = self.client.get(self.url)

        self.assertEqual(response.status_code, status.HTTP_200_OK)
        self.assertEqual(response.data['count'], 0)

```
</details>

&nbsp;

# Ispitivanje sustava
> Ispod se nalaze ispitni slučajevi koji ispituju cijeli sustav. Alat korišten za ispitivanje jest Microsoftov Playwright. Zbog poteškoća sa zaobilaženjem strogih kontrola Google servisa vezanih uz "nedozvoljeno" automatizirano pristupanje uslugama, testiranje povezano uz Google OAuth 2.0 (TEST 8) posebno je izvođeno u Chrome DevTools Recorderu, koji funkcionira vrlo slično kao Playwright.

> Korištenjem Playwright alata za testiranje omogućuje testiranje automatski u tri različita preglednika (Google Chrome, Firefox i Webkit).

## <a id="test7"> TEST 7: Lokalna registracija i uređivanje profila - redovan slučaj</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. **Korisnički podatci**: oni koji su potrebni za <br> registraciju (email, lozinka, korisničko <br> ime, ime i prezime, primatelj ili <br> darovatelj, lokacija, potvrda o punoljetnosti) <br> 2. **Korisničke akcije:** unos podataka u tekstualne <br> okvire, klik (odabir vrste korisnika, potvrda o dobi) | 1. Pritisnuti gumb **Registriraj se** na početnoj stranici. &nbsp; <br> 2. Unijeti email adresu i šifru. <br> 3. Pritisnuti gumb **Registriraj se**. <br> 4. Odabrati ulogu (npr. **donor**). <br> 5. Unijeti ime, prezime i korisničko ime te potvrditi dob. <br> 6. Pritisnuti gumb za dovršetak registracije. <br> 7. Kliknuti na vlastito korisničko ime. <br> 8. Odabrati **Profil**, zatim **Uredi**. <br> 9. Unijeti promjene (ime, prezime, lokacija). <br> 10. Pritisnuti **Spremi promjene**. <br> 11. Potvrditi točnost spremljenih podataka. <br> 12. Odjaviti se. | 1. Kreiran novi profil s točnim <br> unesenim podatcima. <br> 2. Izmjene na profilu (ime, prezime, <br> lokacija) su uspješno pohranjene i <br> vidljive korisniku. |

| Dobiveni izlazi | 
| :--- |
|Test je ostvario sve očekivane izlaze u sva tri različita preglednika.| 
<details><summary> prikaz rezultata testiranja</summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_registracija.png)
![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_registracija_firefox_webkit.png)
</details> 
<details><summary> kod </summary>

```python
import re
from playwright.sync_api import Page, expect

def test_registracija(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava?next=/")
    page.get_by_role("link", name="Registriraj se").click()
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("testiranjeprogi@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Stvori novi račun").click()
    page.get_by_role("button", name="Donator").click()
    page.get_by_role("textbox", name="Unesite ime").click()
    page.get_by_role("textbox", name="Unesite ime").fill("Ivan")
    page.get_by_role("textbox", name="Unesite ime").press("Tab")
    page.get_by_role("textbox", name="Unesite prezime").fill("Horvat")
    page.get_by_role("textbox", name="Unesite korisničko ime").click()
    page.get_by_role("textbox", name="Unesite korisničko ime").fill("testiranjeprogi23")
    page.get_by_role("button", name="Show popup").click()
    page.get_by_text("Ivanić-Grad").click()
    page.get_by_role("checkbox", name="Potvrđujem da sam punoljetna").click()
    page.get_by_role("button", name="Registriraj").click()
    page.get_by_role("heading", name="Pozdrav, Ivan! 👋").click()
    page.get_by_role("button", name="Ivan Horvat Ivan Horvat Donor").click()
    page.get_by_role("menuitem", name="Profil").click()
    page.get_by_role("textbox").first.click()
    page.get_by_role("textbox").first.fill("Marko")
    page.locator("uformgroup").filter(has_text="Prezime:").get_by_role("textbox").click()
    page.locator("uformgroup").filter(has_text="Prezime:").get_by_role("textbox").fill("Kovač")
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").click()
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").click()
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").press("ArrowRight")
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").press("ArrowRight")
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").press("ArrowRight")
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").press("ArrowRight")
    page.locator("uformgroup").filter(has_text="Korisničko ime:").get_by_role("textbox").fill("testiranjeprogi2345")
    page.get_by_role("button", name="Show popup").click()
    page.get_by_text("Rab").click()
    page.get_by_role("button", name="Spremi promjene").click()
    page.get_by_role("link", name="Početna").click()
    page.get_by_role("heading", name="Pozdrav, Marko! 👋").click()
    page.get_by_role("button", name="Marko Kovač Marko Kovač Donor").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()
```

</details> 

## <a id="test8"> TEST 8: Prijava i odjava korištenjem OAuth 2.0 </a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Korisnički podatci: podatci potrebni za Google račun (lozinka i email adresa) <br> 2. Korisničke akcije: prijava, odjava, klik na meni unutar aplikacije | 1. Otvoriti domenu stranice i pritisnuti gumb za prijavu pomoću Google računa. <br> 2. Unijeti podatke Google računa i tako ući u sustav. <br> 3. Pritisnuti meni “Profil”. <br> 4. Pritisnuti “Odjava” na meniju “Profil”. <br> 5. Pritisnuti “Idi na prijavu” nakon izlaska. <br> | 1. Korisnik uspješno prijavljen u aplikaciju. <br> 2. Korisnik uspješno odjavljen iz aplikacije. <br> 3. Korisnik se nalazi na istoj domeni na kojoj je i započeo prijavu. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika.|

<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Chrome DevTools Recorder](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_oauth.png)

</details>



<details><summary> kod </summary>

```js
const puppeteer = require('puppeteer'); // v23.0.0 or later

(async () => {
    const browser = await puppeteer.launch();
    const page = await browser.newPage();
    const timeout = 5000;
    page.setDefaultTimeout(timeout);

    {
        const targetPage = page;
        await targetPage.setViewport({
            width: 1075,
            height: 919
        })
    }
    {
        const targetPage = page;
        await targetPage.goto('https://sandbox.playforward.dedyn.io/prijava');
    }
    {
        const targetPage = page;
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Prijava s Google računom)'),
            targetPage.locator('div > button'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app\\"]/div/div/div/div[2]/div/div/div/button)'),
            targetPage.locator(':scope >>> div > button'),
            targetPage.locator('::-p-text(Prijava s Google)')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 391.5,
                y: 37.25,
              },
            });
        await Promise.all(promises);
    }
    {
        const targetPage = page;
        const promises = [];
        const startWaitingForEvents = () => {
            promises.push(targetPage.waitForNavigation());
        }
        await puppeteer.Locator.race([
            targetPage.locator('li:nth-of-type(2) div.pGzURd'),
            targetPage.locator('::-p-xpath(//*[@id=\\"yDmH0d\\"]/c-wiz/main/div[2]/div/div/div[1]/span/section/div/div/div/div/ul/li[2]/div/div[1]/div/div[2]/div[1])'),
            targetPage.locator(':scope >>> li:nth-of-type(2) div.pGzURd')
        ])
            .setTimeout(timeout)
            .on('action', () => startWaitingForEvents())
            .click({
              offset: {
                x: 24.5,
                y: 16.5,
              },
            });
        await Promise.all(promises);
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('p.text-highlighted'),
            targetPage.locator('::-p-xpath(//*[@id=\\"reka-dropdown-menu-trigger-v-0\\"]/div/div/p[1])'),
            targetPage.locator(':scope >>> p.text-highlighted'),
            targetPage.locator('::-p-text(Ivan Džepina)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 72,
                y: 9,
              },
            });
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('a:nth-of-type(2) > span.flex-1 > span'),
            targetPage.locator('::-p-xpath(//*[@id=\\"reka-dropdown-menu-content-v-1\\"]/div/div/a[2]/span[1]/span)'),
            targetPage.locator(':scope >>> a:nth-of-type(2) > span.flex-1 > span'),
            targetPage.locator('::-p-text(Odjava)')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 43,
                y: 11,
              },
            });
    }
    {
        const targetPage = page;
        await puppeteer.Locator.race([
            targetPage.locator('::-p-aria(Idi na prijavu) >>>> ::-p-aria([role=\\"image\\"])'),
            targetPage.locator('svg'),
            targetPage.locator('::-p-xpath(//*[@id=\\"app\\"]/div/div[4]/div/div/div/a/svg)'),
            targetPage.locator(':scope >>> svg')
        ])
            .setTimeout(timeout)
            .click({
              offset: {
                x: 12.984375,
                y: 21.5,
              },
            });
    }

    await browser.close();

})().catch(err => {
    console.error(err);
    process.exit(1);
});
```
</details>



## <a id="test9"> TEST 9: Objava, uređivanje i brisanje oglasa </a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Korisnički podatci: podatci potrebni za <br> izradu oglasa (ime i opis oglasa, slika <br> igračke, lokacija, preferencija preuzimanja <br> oglasa, stanje igračke, vrsta igračke) <br> 2. Korisničke akcije: klik na gumb za kreiranje/ <br> uređivanje/objavu oglasa, unos podataka u tekstualne okvire ili menije | 1. Otvoriti sučelje za kreiranje oglasa. <br> 2. Ispuniti potrebne podatke za stvaranje oglasa (ime i opis oglasa, slika igračke itd.) <br> 3. Pritisnuti gumb “Objavi oglas” <br> 4. Na sučelju aplikacije gdje su vidljivi vlastiti oglasi pritisnuti gumb “Uredi oglas” <br> 5. Promijeniti podatke koji su uneseni ranije. <br> 6. Ponovo objaviti oglas kao u (3.) <br> 7. Na sučelju aplikacije gdje su vidljivi vlastiti oglasi pritisnuti gumb “Izbriši oglas” i izbrisati oglas. | 1. Darivatelj uspješno stvorio oglas. <br> 2. Darivatelj uspješno izmijenio oglas. <br> 3. Darivatelj uspješno izbrisao oglas. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika. |
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_oglas.png)

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_oglas_firefox_webkit.png)

</details>
<details><summary> kod </summary>


```python
import re
from playwright.sync_api import Page, expect


def test_oglas(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava?next=/")
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("test@test.test")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Prijava", exact=True).click()
    page.get_by_role("link", name="Moji oglasi").click()
    page.get_by_role("link", name="Novi oglas").click()
    page.get_by_role("textbox", name="Naslov").click()
    page.get_by_role("textbox", name="Naslov").fill("Testna igračka")
    with page.expect_file_chooser() as fc_info:
        page.get_by_text("Dodajte ručno ili povucite").click()
    file_chooser = fc_info.value
    file_chooser.set_files("C:\\Users\\Milan\\Desktop\\Progi TESTOVI\\test_slika.jpg")
    page.get_by_role("textbox", name="Unesite opis igračke").click()
    page.get_by_role("textbox", name="Unesite opis igračke").fill("Lijepa igračka u dobrom stanju.")
    page.locator("button").filter(has_text="Odaberite kategoriju").click()
    page.get_by_text("Kocke i konstrukcijske igračke").click()
    page.locator("button").filter(has_text="Korčula").click()
    page.get_by_text("Knin").click()
    page.locator("label").filter(has_text="Rabljeno").click()
    page.get_by_text("Osobno preuzimanje").click()
    page.get_by_role("button", name="Pregledaj i objavi oglas").click()
    page.get_by_role("button", name="Objavi").click()
    page.get_by_role("link", name="Moji oglasi").click()


    page.reload()
    element = page.get_by_role("link", name="Uredi oglas")
    element.click()


    # page.get_by_role("link", name="Uredi oglas").click()
    page.get_by_role("textbox", name="Unesite opis igračke").click()
    page.get_by_role("textbox", name="Unesite opis igračke").press("ArrowRight")
    page.get_by_role("textbox", name="Unesite opis igračke").press("ArrowRight")
    page.get_by_role("textbox", name="Unesite opis igračke").press("ArrowRight")
    page.get_by_role("textbox", name="Unesite opis igračke").press("ArrowRight")
    page.get_by_role("textbox", name="Unesite opis igračke").fill("Lijepa igračka u dobrom stanju. Izmijenjen opis.")
    page.get_by_role("textbox", name="Naslov").click()
    page.get_by_role("textbox", name="Naslov").fill("Testna igračka, promijenjen oglas")
    page.locator("button").filter(has_text="Knin").click()
    page.get_by_text("Klanjec").click()
    page.get_by_role("radio", name="Obnovljeno").click()
    page.get_by_role("button", name="Pregledaj i objavi oglas").click()
    page.get_by_role("button", name="Objavi").click()
    page.get_by_role("heading", name="Testna igračka, promijenjen").click()
    page.get_by_role("heading", name="Kocke i konstrukcijske igračke").click()
    page.get_by_role("heading", name="Obnovljeno").click()
    page.get_by_role("heading", name="Klanjec").click()
    #page.get_by_text("Lijepa igračka u dobrom").click()

    element = page.get_by_text("Testna igračka")

    # Explicitly wait until it is visible
    element.wait_for(state="attached")
    element.scroll_into_view_if_needed()
    element.wait_for(state="visible", timeout=10_000)
    # Ensure it is scrolled into view
    # page.reload()

    # Interact with it
    element.click()
    page.get_by_role("button", name="Obriši oglas").click()
    page.get_by_role("button", name="Obriši").click()
    page.get_by_role("heading", name="Još nemate oglasa").click()
    page.get_by_role("button", name="Test Testić Test Testić Donor").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()

```
</details>

## <a id="test10"> TEST 10: Objava, uređivanje i brisanje kampanji</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Trenutna sesija prijavljena na izrađen račun udruge. <br> 2. Korisnički podatci: podatci potrebni za izradu kampanje (ime, opis i trajanje kampanje, lokacija, vrsta i količina potrebnih igračaka) <br> 3. Korisničke akcije: klik na gumb za kreiranje/uređivanje/objavu kampanja, unos podataka u tekstualne okvire ili menije| 1. Otvoriti sučelje za kreiranje kampanji. <br> 2. Ispuniti potrebne podatke za stvaranje kampanji. <br> 3. Pritisnuti gumb “Objavi kampanju” <br> 4. Na sučelju aplikacije gdje su vidljivi vlastite kampanje pritisnuti gumb “Uredi kampanje” <br> 5. Promijeniti podatke koji su uneseni ranije. <br> 6. Ponovo objaviti kampanju kao u (3.) <br> 7. Na sučelju aplikacije gdje su vidljivi vlastite kampanje pritisnuti gumb “Izbriši kampanje” i izbrisati kampanju. | 1. Udruga uspješno stvorila kampanju. <br> 2. Udruga uspješno izmijenila kampanju. <br> 3. Udruga uspješno izbrisala kampanju. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika. |
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Chrome DevTools Recorder](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_kampanja.png)

![Prikaz dnevnika alata za testiranje Chrome DevTools Recorder](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_kampanja_firefox_webkit.png)

</details>
<details><summary> kod </summary>


```python

import re
from playwright.sync_api import Page, expect


def test_kampanja(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava")
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("dz1v4n@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Prijava", exact=True).click()
    page.get_by_role("link", name="Moje kampanje").click()
    page.get_by_role("link", name="Nova kampanja").click()
    page.get_by_role("textbox", name="Naslov").click()
    page.get_by_role("textbox", name="Naslov").fill("Test kampanja")
    with page.expect_file_chooser() as fc_info:
        page.get_by_text("Dodajte ručno ili povucite").click()
    file_chooser = fc_info.value
    file_chooser.set_files("C:\\Users\\Milan\\Desktop\\Progi TESTOVI\\test_slika.jpg")
    page.get_by_role("textbox", name="Opis kampanje").click()
    page.get_by_role("textbox", name="Opis kampanje").fill("Test opis kampanje.")
    page.get_by_role("button", name="Show popup").click()
    page.get_by_text("Slatina").click()
    page.get_by_role("textbox", name="Datum završetka kampanje").fill("2027-07-07")
    page.get_by_role("textbox", name="Naziv igračke").click()
    page.get_by_role("textbox", name="Naziv igračke").fill("igracka1")
    page.get_by_role("button", name="Dodaj igračku").click()
    page.get_by_role("button", name="Dodaj igračku").click()
    page.locator("#v-273").click()
    page.locator("#v-273").fill("igracka2")
    page.locator("#v-274").click()
    page.locator("#v-274").fill("igračka3")
    page.get_by_role("spinbutton").nth(2).click()
    page.get_by_role("spinbutton").nth(2).fill("14")
    page.get_by_role("button", name="Pregledaj i objavi kampanju").click()
    page.get_by_text("igracka1").click()
    page.get_by_text("igracka2").click()
    page.get_by_text("igračka3").click()
    page.get_by_text("Test opis kampanje.").click()
    page.get_by_role("button", name="Objavi").click()
    page.get_by_role("link", name="Uredi kampanju").click()
    page.get_by_role("textbox", name="Opis kampanje").click()
    page.get_by_role("textbox", name="Opis kampanje").fill("Test opis kampanje. Promjena")
    page.get_by_role("textbox", name="Naslov").click()
    page.get_by_role("textbox", name="Naslov").fill("Test kampanja promjena")
    page.get_by_role("spinbutton").first.click()
    page.get_by_role("spinbutton").first.fill("12")
    page.locator("#v-281").click()
    page.locator("#v-281").fill("igracka123")
    page.get_by_role("button", name="Pregledaj i objavi kampanju").click()
    page.get_by_role("button", name="Objavi").click()
    page.get_by_text("Test opis kampanje. Promjena").click()
    page.get_by_role("heading", name="Test kampanja promjena").click()
    page.get_by_role("button", name="Obriši kampanju").click()
    page.get_by_role("button", name="Obriši").click()
    page.get_by_role("heading", name="Još nemate kampanje").click()
    page.get_by_role("button", name="Ivan Horvat Ivan Horvat Udruga").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()


```
</details>

## <a id="test11"> TEST 11: Slanje i primanje poruka i zahtjeva putem chata</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Korisnički podatci: tekst koji se želi poslati, podatci za prijavu primatelja i darovatelja <br>2. Korisničke akcije: klik na gumb za chatanje s drugim korisnikom, upis teksta, klik na dugme za slanje zahtjev i klik za prihvaćanje zahtjeva| 1. Prijaviti se na račun primatelja.<br> 2. Otvoriti određeni oglas.<br>3. Pritisnuti gumb “Započni chat”.<br>4. Upisati željeni tekst u kućicu za upis teksta.<br>5. Pritisnuti gumb za slanje poruke.<br>6. Pritisnuti gumb za slanje zahtjeva.<br>7. Odabrati vrstu zahtjeva.<br>8. Poslati zahtjev.<br>9. Prebaciti se na račun osobe čiji oglas je otvoren.<br>10. Prihvatiti zahtjev za donaciju.<br>11. Poslati tekstualnu poruku primatelju.<br>12. Odjaviti se. |1. Primatelj i darovatelj uspješno ušli u sučelje za chat.<br>2. Primatelj i darovatelj uspješno poslali poruke u chat.<br>3. Darovatelj vidi poslanu poruku i poslan zahtjev na svom računu.<br>4. Darovatelj uspješno odgovara na poruku. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika. |
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_chat.png)

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_chat_firefox_webkit.png)

</details>
<details><summary> kod </summary>


```python

import re
from playwright.sync_api import Page, expect

def test_chat(page: Page) -> None:
   page.goto('https://sandbox.playforward.dedyn.io/prijava?next=/')
   page.get_by_role('textbox',  name= 'E-mail*' ).click()
   page.get_by_role('textbox',  name= 'E-mail*' ).fill('testiranjeprogi@gmail.com')
   page.get_by_role('textbox',  name= 'E-mail*' ).press('Tab')
   page.get_by_role('textbox',  name= 'Lozinka*' ).fill('sifra1234')
   page.get_by_role('button',  name= 'Prijava', exact=True ).click()
   page.get_by_role('heading',  name= 'Oglas za testiranje chata' ).click()
   page.get_by_role('button',  name= 'Započni razgovor' ).click()
   page.get_by_role('textbox',  name= 'Napišite poruku...' ).click()
   page.get_by_role('textbox',  name= 'Napišite poruku...' ).fill('Molim Vas za donaciju.')
   page.get_by_role("button").filter(has_text=re.compile(r"^$")).click()
   page.get_by_role('button',  name= 'Zahtjev' ).click()
   page.get_by_role('radio',  name= 'Osobno preuzimanje' ).click()
   page.get_by_role('button',  name= 'Pošalji zahtjev' ).click()
   page.get_by_role('button',  name= 'Ivan Horvat Ivan Horvat Udruga' ).click()
   page.get_by_role('menuitem',  name= 'Odjava' ).click()
   page.get_by_role('link', name = 'Idi na prijavu' ).click()
   page.get_by_role('textbox', name = 'E-mail*' ).click()
   page.get_by_role('textbox', name = 'E-mail*' ).fill('test@test.test')
   page.get_by_role('textbox', name = 'E-mail*' ).press('Tab')
   page.get_by_role('textbox', name = 'Lozinka*' ).fill('sifra1234')
   page.get_by_role('button', name = 'Prijava', exact=True ).click()
   page.get_by_role('link', name = 'Moji oglasi' ).click()
   page.get_by_role('link', name = 'Razgovori' ).click()
   page.get_by_role('button', name = 'Oglas za testiranje chata' ).click()
   page.reload()
   page.get_by_role('textbox', name = 'Napišite poruku...' ).click()
   page.get_by_role('textbox', name = 'Napišite poruku...' ).fill('Nema problema! Molim Vas da se nađemo u petak u 11.')
   page.get_by_role("button").filter(has_text=re.compile(r"^$")).click()
   page.get_by_role('button', name = 'Prihvati donaciju' ).click()
   page.get_by_text('Molim Vas za donaciju.').click()
   page.get_by_role('button', name = 'Test Testić Test Testić Donor' ).click()
   page.get_by_role('menuitem', name = 'Odjava' ).click()
   page.get_by_role('link', name = 'Idi na prijavu' ).click()
   page.get_by_role('textbox', name = 'E-mail*' ).click()
   page.get_by_role('textbox', name = 'E-mail*' ).fill('testiranjeprogi@gmail.com')
   page.get_by_role('textbox', name = 'E-mail*' ).press('Tab')
   page.get_by_role('textbox', name = 'Lozinka*' ).fill('sifra1234')
   page.get_by_role('button', name = 'Prijava', exact=True ).click()
   page.get_by_role('link', name = 'Razgovori' ).click()
   page.get_by_role('button', name = 'Oglas za testiranje chata' ).click()
   page.reload()
   page.get_by_role('heading', name = 'Donacija prihvaćena' ).click()
   page.get_by_text('Nema problema! Molim Vas da').click()
   page.get_by_role('button', name = 'Ivan Horvat Ivan Horvat Udruga' ).click()
   page.get_by_role('menuitem', name = 'Odjava' ).click()
   page.get_by_role('link', name = 'Idi na prijavu' ).click()

```
</details>


## <a id="test12"> TEST 10: Administratorsko sučelje - uklanjanje sadržaja</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Administratorski podatci: korisničko ime i šifra za prijavu. <br>2. Administratorske akcije: klik na dugmad za prijavu na admin sučelje, klik za odabir sadržaja kojeg se želi ukloniti, klik na gumb za potvrdu uklanjanja| 1. Registrirati račun kao udruga.<br>2. Kao udruga stvoriti kampanju.<br>3. Odjaviti se iz korisničkog i ulogirati u admin sučelje.<br>4. U admin sučelju izbrisati kampanju.<br>5. U admin sučelju izbrisati korisnika - udrugu.<br>6. Odjaviti se iz admin sučelja. | Udruga i oglas uspješno izbrisani. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika. |
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_admin.png)

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_admin_firefox_webkit.png)

</details>
<details><summary> kod </summary>


```python

import re
from playwright.sync_api import Page, expect


def test_admin(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava?next=/")
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("link", name="Registriraj se").click()
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("dz1v4n@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Stvori novi račun").click()
    page.get_by_role("button", name="Primatelj").click()
    page.get_by_role("button", name="Udruga").click()
    page.get_by_role("textbox", name="Unesite ime").click()
    page.get_by_role("textbox", name="Unesite ime").fill("test")
    page.get_by_role("textbox", name="Unesite prezime").click()
    page.get_by_role("textbox", name="Unesite prezime").fill("test")
    page.get_by_role("textbox", name="Unesite korisničko ime").click()
    page.get_by_role("textbox", name="Unesite korisničko ime").fill("udruga12")
    page.get_by_role("button", name="Show popup").click()
    page.get_by_role("option", name="Dubrovnik").click()
    page.get_by_role("checkbox", name="Potvrđujem da sam punoljetna").click()
    page.get_by_role("button", name="Nastavak ➜").click()
    page.get_by_role("textbox", name="Ime udruge").click()
    page.get_by_role("textbox", name="Ime udruge").fill("ivan.dzepina1407@gmail.com")
    page.get_by_role("textbox", name="Ime udruge").press("Tab")
    page.get_by_role("textbox", name="Email udruge").fill("Mali testi")
    page.get_by_role("textbox", name="Ime udruge").click()
    page.get_by_role("textbox", name="Ime udruge").press("ControlOrMeta+a")
    page.get_by_role("textbox", name="Ime udruge").fill("Mali kaktusi")
    page.get_by_role("textbox", name="Email udruge").click()
    page.get_by_role("textbox", name="Email udruge").press("ControlOrMeta+a")
    page.get_by_role("textbox", name="Email udruge").fill("ivan.dzepina1407@gmail.com")
    page.get_by_role("button", name="Registriraj").click()
    page.get_by_role("link", name="Moje kampanje").click()
    page.get_by_role("link", name="Nova kampanja").click()
    page.get_by_role("textbox", name="Naslov").click()
    page.get_by_role("textbox", name="Naslov").fill("test kampanja")
    with page.expect_file_chooser() as fc_info:
        page.get_by_text("Dodajte ručno ili povucite").click()
    file_chooser = fc_info.value
    file_chooser.set_files("C:\\Users\\Milan\\Desktop\\Progi TESTOVI\\test_slika.jpg")
    page.get_by_role("textbox", name="Opis kampanje").click()
    page.get_by_role("textbox", name="Opis kampanje").fill("test")
    page.get_by_role("button", name="Show popup").click()
    page.get_by_text("Makarska").click()
    page.get_by_role("textbox", name="Datum završetka kampanje").fill("2027-05-05")
    page.get_by_role("textbox", name="Naziv igračke").click()
    page.get_by_role("textbox", name="Naziv igračke").fill("gggggg")
    page.get_by_role("button", name="Dodaj igračku").click()
    page.locator("#v-540").click()
    page.locator("#v-540").fill("asdasadsd")
    page.get_by_role("button", name="Dodaj igračku").click()
    page.locator("#v-541").click()
    page.locator("#v-541").fill("asdasdasd")
    page.get_by_role("button", name="Pregledaj i objavi kampanju").click()
    page.get_by_role("button", name="Objavi").click()
    page.get_by_role("button", name="test test test test Udruga").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.goto("https://sandbox.playforward.dedyn.io/admin/")
    page.get_by_role("textbox", name="Email address *").click()
    page.get_by_role("textbox", name="Email address *").fill("admin@playforward.dedyn.io")
    page.get_by_role("textbox", name="Password *").click()
    page.get_by_role("textbox", name="Password *").fill("XXXX")
    page.get_by_role("button", name="Log in arrow_forward").click()
    page.locator("#link-campaigns-campaign").click()
    page.get_by_role("link", name="test kampanja", exact=True).click()
    page.get_by_role("link", name="Delete campaign").click()
    page.get_by_role("button", name="Yes, I’m sure").click()
    page.locator("#link-users-user").click()
    page.get_by_role("link", name="dz1v4n@gmail.com").click()
    page.get_by_role("link", name="Delete user").click()
    page.get_by_role("button", name="Yes, I’m sure").click()
    page.get_by_text("admin@playforward.dedyn.io").first.click()
    page.get_by_role("button", name="Log out logout").click()


```
</details>

## <a id="test14"> TEST 14: Vršenje plaćanja dostave i ostavljanje potvrde i recenzije</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Korisnički podatci: testni podatci za slanje sredstava za dostavu putem Stripe sučelja, oglas čiji je zahtjev za donaciju dostavom potvrđen <br>2. Korisničke akcije: klik na dugme za zatraživanje uplate, unos testnih podataka u Stripe sučelje za naplatu| 1. Kao darovatelj, dugmetom “Naplati dostavu” odrediti cijenu dostave.<br>2. Odjaviti se i prijaviti se kao primatelj.<br>3.  Kao primatelj, otvoriti određen oglas gdje je darovatelj zatražio uplatu sredstava za dostavu. <br>4. Pritisnuti dugme za plaćanje (ulazak u Stripe sučelje za plaćanje).<br>5. Unijeti podatke potrebne za plaćanje.<br>6. Provesti plaćanje.<br>7. Dobiti potvrdu o provedenom plaćanju.<br>8. Na sekciji “Aktivne donacije” pritisnuti gumb “Potvrdi donaciju”.<br>9. Ostaviti recenziju.<br>10. Pritisnuti gumb “Pošalji recenziju”.<br>11. Odjavi se. | 1. Primatelj uspješno ušao u sučelje za naplatu.<br>2. Primatelj proveo plaćanje s pomoću Stripe sučelja. <br>3. Sustav registrirao potvrdu donacije i omogućio ostavljanje recenzije.<br>4. Sustav ostavio recenziju.|

| Dobiveni izlazi | 
| :--- | 
| Test funkcionira kad se ispituje ručno. Nažalost, zbog toga što smo imali poteškoća s lociranjem Stripe-ovih elemenata na stranici za plaćanje, u jednom trenutku naplate PlayWright bez obzira na mnoge pokušaje zastane. Stoga automatizirano ispitivanje ovog testa nije bilo moguće. Kod koji se izvršio do problematične linije i cijeli kod testa nalazi se ispod. 
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_placanje_recenzije.png)


</details>
<details><summary> kod </summary>


```python

import re
from playwright.sync_api import Page, expect


def test_placanje_recenzije(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava")
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("dzivan1407@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Prijava", exact=True).click()
    page.get_by_role("link", name="Razgovori").click()
    page.get_by_role("button", name="test12 test12 dz1v4n").click()
    page.get_by_role("button", name="Naplati dostavu").click()
    page.get_by_role("spinbutton", name="Iznos dostave (€)").click()
    page.get_by_role("spinbutton", name="Iznos dostave (€)").fill("3")
    page.get_by_role("button", name="Pošalji").click()
    page.get_by_role("button", name="test test test test Donor").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("dz1v4n@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Prijava", exact=True).click()
    page.get_by_role("link", name="Razgovori").click()
    page.get_by_role("button", name="test12 test12 test").click()
    with page.expect_popup() as page2_info:
        page.get_by_role("button", name="Plati").click()
    page2 = page2_info.value
    page2.get_by_role("textbox", name="Email").click()
    page2.get_by_role("textbox", name="Email").fill("test@gmail.com")
    page2.get_by_text("Card").dblclick()
    page2.get_by_role("textbox", name="Card number").fill("4242 4242 4242 4242")
    page2.get_by_text("Card").dblclick()
    page2.get_by_role("textbox", name="Expiration").fill("01 / 29")
    page2.get_by_role("textbox", name="CVC").fill("123")
    page2.get_by_role("textbox", name="Cardholder name").fill("test placanje")
    page2.get_by_test_id("hosted-payment-submit-button").click()
    page2.get_by_text("Thanks for your payment").click()
    page.get_by_role("heading", name="Plaćena dostava").click()
    page.get_by_role("link", name="Aktivne donacije").click()
    page.get_by_role("button", name="Potvrdi primopredaju").nth(1).click()
    page.get_by_role("button", name="Potvrdi primopredaju").click()
    page.get_by_role("button").nth(4).click()
    page.locator("div").filter(has_text=re.compile(r"^Komentar$")).nth(1).click()
    page.get_by_role("textbox", name="Komentar").click()
    page.get_by_role("textbox", name="Komentar").fill("super iskustvo")
    page.get_by_role("button", name="Pošalji recenziju").click()
    page.get_by_role("button", name="test test test test Udruga").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()


```
</details>

## <a id="test15"> TEST 15: Polovično izvršeno plaćanje</a>
| Ulazi | Koraci | Očekivani izlaz |
| :--- | :--- | :--- |
| 1. Trenutna sesija prijavljena na izrađen račun udruge. <br> 2. Korisnički podatci: podatci potrebni za izradu kampanje (ime, opis i trajanje kampanje, lokacija, vrsta i količina potrebnih igračaka) <br> 3. Korisničke akcije: klik na gumb za kreiranje/uređivanje/objavu kampanja, unos podataka u tekstualne okvire ili menije| 1. Otvoriti sučelje za kreiranje kampanji. <br> 2. Ispuniti potrebne podatke za stvaranje kampanji. <br> 3. Pritisnuti gumb “Objavi kampanju” <br> 4. Na sučelju aplikacije gdje su vidljivi vlastite kampanje pritisnuti gumb “Uredi kampanje” <br> 5. Promijeniti podatke koji su uneseni ranije. <br> 6. Ponovo objaviti kampanju kao u (3.) <br> 7. Na sučelju aplikacije gdje su vidljivi vlastite kampanje pritisnuti gumb “Izbriši kampanje” i izbrisati kampanju. | 1. Udruga uspješno stvorila kampanju. <br> 2. Udruga uspješno izmijenila kampanju. <br> 3. Udruga uspješno izbrisala kampanju. |

| Dobiveni izlazi | 
| :--- | 
| Test je ostvario sve očekivane izlaze u sva tri različita preglednika. |
<details><summary> prikaz rezultata testiranja </summary>

![Prikaz dnevnika alata za testiranje Playwright](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_rubni.png)

![Prikaz dnevnika alata za testiranje Playwright]](https://github.com/maucikamau/GiftHub/blob/devdocs/testiranja/test_rubni_firefox_webkit.png)

</details>
<details><summary> kod </summary>


```python

import re
from playwright.sync_api import Page, expect


def test_rubni(page: Page) -> None:
    page.goto("https://sandbox.playforward.dedyn.io/prijava?next=/")
    page.get_by_role("textbox", name="E-mail*").click()
    page.get_by_role("textbox", name="E-mail*").fill("dz1v4n@gmail.com")
    page.get_by_role("textbox", name="E-mail*").press("Tab")
    page.get_by_role("textbox", name="Lozinka*").fill("sifra1234")
    page.get_by_role("button", name="Prijava", exact=True).click()
    page.get_by_role("link", name="Razgovori").click()
    page.get_by_role("button", name="test11 test11 test").click()
    with page.expect_popup() as page1_info:
        page.get_by_role("button", name="Plati").click()
    page1 = page1_info.value
    page1.get_by_role("textbox", name="Email").click()
    with page.expect_popup() as page2_info:
        page.get_by_role("button", name="Plati").click()
    page2 = page2_info.value
    page.get_by_role("button", name="test test test test Udruga").click()
    page.get_by_role("menuitem", name="Odjava").click()
    page.get_by_role("link", name="Idi na prijavu").click()


```
</details>